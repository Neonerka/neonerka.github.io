version: '3.8'

services:
  bio-site:
    build: .
    container_name: neonerka_bio_site
    ports:
      - "80:80"
    restart: always # Ключевой параметр для автозапуска контейнера вместе с системой

